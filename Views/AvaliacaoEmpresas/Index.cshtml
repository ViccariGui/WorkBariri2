@model IEnumerable<WorkBariri2.Models.AvaliacaoEmpresa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Feedback)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EscalaEstrela)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsuariosId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empresas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Feedback)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EscalaEstrela)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuariosId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresas.EmpresasId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvaliacaoEmpresaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvaliacaoEmpresaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvaliacaoEmpresaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
